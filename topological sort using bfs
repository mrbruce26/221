import queue
inp = open("inputmax.txt", "r")
out = open("outputmax.txt", "w")
x,y = inp.readline().split(" ")

r = []

for i in range(int(y)):
    edge = inp.readline().strip().split(" ")
    r.append(list(map(int,edge)))
dict={}
dict1 = {}
for i in range(1,int(x)+1):
    dict[i]=[]
    dict1[i]=[]
for i in r:
    dict[i[0]].append(i[1])
    dict1[i[1]].append(i[0])
q = []
q.append(1)
visited = {}
top= []
res=[]
for i in dict1.keys():
    if len(dict1[i])==0:
        top.append(i)
for i in dict.keys():
    if len(top)!=0:
        c = top.pop(0)
        s = dict[c]
        for i in s:
            dict1[i].remove(c)
            if len(dict1[i])== 0:
                top.append(i) 
        res.append(c)

print("Impossible")

# out.write(f'{res}')

